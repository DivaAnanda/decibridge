# Root-level env for Docker Compose. Copy to .env.
# Backend/frontend each have their own .env.example for native dev.

POSTGRES_USER=decibridge
POSTGRES_PASSWORD=decibridge
POSTGRES_DB=decibridge

DJANGO_SECRET_KEY=dev-only-secret-replace-in-prod
DJANGO_DEBUG=True
